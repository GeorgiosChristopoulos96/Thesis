{
  "output_dir": "./rdf_to_text_model_DUMMY",
  "evaluation_strategy": "steps",
  "learning_rate": 0.001,
  "per_device_train_batch_size": 4,
  "per_device_eval_batch_size": 4,
  "num_train_epochs": 5,
  "predict_with_generate": true,
  "save_strategy": "steps",
  "save_total_limit": 1,
  "optim": "adafactor",
  "use_mps_device": true,
  "max_steps": 25000,
  "eval_steps": 200,
  "logging_steps": 200,
  "save_steps": 1000
}
